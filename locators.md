(By.XPATH, "//p[text()='Личный Кабинет']")                          # Кнопка перехода в Личный Кабинет в шапке.
(By.XPATH, "//div[label[text()='Имя']]/input")                      # Поле ввода Имя в форме регистрации
(By.XPATH, "//div[label[text()='Email']]/input")                    # Поле ввода Логина в форме регистрации
(By.XPATH, "//div[label[text()='Пароль']]/input")                   # Поле ввода Пароля в форме регистрации
(By.XPATH, "//a[text()='Зарегистрироваться']")                      # Кнопка Зарегистрироваться после ввода Имени, Логина и Пароля для создания аккаунта
(By.XPATH, "//button[text()='Войти']")                              # Кнопка Войти после ввода зарегистрированного аккаунта
(By.XPATH, "//p[text()='Некорректный пароль']")                     # Текст ошибки при вводе невалидного пароля 
(By.XPATH, "//p[text()='Личный Кабинет']")                          # Кнопка перехода в профиль в верхней шапке сайта
(By.XPATH, "//p[text()='Конструктор']")                             # Кнопка возврата на главную страницу в верхней шапке сайта
(By.XPATH, "//a[@href='/']")                                        # Логотип «Stellar Burgers» в шапке для перехода на главную страницу
(By.XPATH, "//button[text()='Оформить заказ']")                     # Кнопка на главной странице (главный маркер успешного входа в систему)
(By.XPATH, "//button[text()='Сохранить']")                          # Кнопка в профиле (маркер того, что Личный кабинет полностью загрузился)
(By.XPATH, "//button[text()='Выход']")                              # Кнопка деавторизации и выхода из системы внутри Личного кабинета
(By.XPATH, "//span[text()='Булки']")                                # Вкладка переключения меню на булки
(By.XPATH, "//span[text()='Соусы']")                                # Вкладка переключения меню на соусы
(By.XPATH, "//span[text()='Начинки']")                              # Вкладка переключения меню на начинки
(By.XPATH, "//img[@alt='Флюоресцентная булка R2-D3']")              # Картинка булки (маркер успешного открытия раздела булок)
(By.XPATH, "//img[@alt='Соус Spicy-X']")                            # Картинка соуса (маркер успешного открытия раздела соусов)
(By.XPATH, "//img[@alt='Мясо бессмертных моллюсков Protostomia']")  # Картинка начинки (маркер успешного открытия раздела начинок)